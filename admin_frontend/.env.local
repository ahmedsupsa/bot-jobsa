# API base is intentionally empty — all /api/* requests are proxied to the
# Flask backend via the rewrite rule in next.config.mjs.
# This eliminates CORS issues and ensures Flask session cookies work correctly.
NEXT_PUBLIC_ADMIN_API_BASE=
