# خادم Flask (لوحة API + تسجيل الدخول). يجب أن يُضبط عند البناء على Railway أيضاً.
ADMIN_API_ORIGIN=https://admin-web-production.up.railway.app

# اتركه فارغاً عند استخدام الـ rewrites أعلاه (طلبات /api/* من نفس دومين الواجهة).
NEXT_PUBLIC_ADMIN_API_BASE=
